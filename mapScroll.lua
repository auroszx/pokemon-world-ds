-- Librería usada para el desplazamiento del mapa.
-- Resulta mas razonable desplazar el mapa y no al jugador.

-- Library used to scroll loaded maps.
-- It makes more sense to scroll the map instead of moving the player.

mapPosX = 0
mapPosY = 0
